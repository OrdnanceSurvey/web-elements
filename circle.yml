machine:
  node:
    version: "5.1.0"
test:
  pre:
    - npm run build
  override:
    - npm test
deployment:
  release:
    branch: master
    commands:
      - npm run semantic-release || true
      - npm run docs-deploy || true

machine:
  node:
    version: "5.1.0"
test:
  pre:
    - npm run build
  override:
    - npm run test
deployment:
  releaseMasterBranch:
    branch: master
    commands:
      - npm run semantic-release || true
